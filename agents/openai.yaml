interface:
  display_name: "B2B Contact Enrichment"
  short_description: "Complete every lead and return JSON in chat"
  default_prompt: "Use $b2b-contact-enrichment to add a complete verified current employee to every company and return the full JSON in chat."
